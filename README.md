# ranking-service
A command-line application that will calculate the ranking table for a soccer league.

## Running locally

Clone to a directory on your local machine.

### Prerequisites

* Java 1.8
* Maven

### Run

```shell
cd ranking-service
mvn clean package
java -cp ./target/classes/ com.spandigital.Main
```
